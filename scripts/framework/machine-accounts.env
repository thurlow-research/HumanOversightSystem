# Machine-account identity + authority config (AGENT-IDENTITY.md §7) — sourced by
# the server-side enforcement (require_human_approval.py) and the setup tooling.
#
# This file is hosting-AGNOSTIC: it works for a personal repo + collaborators
# (the default) or a GitHub org + team, because it identifies the bots by their
# usernames rather than assuming a team. A consumer copies this and fills in
# their own bot usernames.

# ── Bot accounts (the two identity classes, §7) ───────────────────────────────
# GitHub App bot logins. GitHub Apps authenticate as installations and appear
# with a "[bot]" suffix in all API responses and UI. These replaced the former
# PAT machine-user accounts (HOSWorkerTutelare / HOSOversightTutelare) after
# those accounts were flagged by GitHub for bot-like behavior (#541, 2026-06-18).
#
# Auth flow: source <(bootstrap/get_app_token.sh --app worker|oversight)
# This exports GH_TOKEN (installation token) and HOS_BOT_LOGIN into the shell.
# Identity guards must check HOS_BOT_LOGIN (not `gh api user`) — GitHub App
# installation tokens return an error on GET /user.
#
# An approval counts as a HUMAN approval only if the approver is NOT a bot. A
# reviewer is treated as a bot if GitHub reports user.type == "Bot", the login
# ends in "[bot]", OR the login is listed in BOT_ACCOUNTS (case-insensitive).
# BOT_ACCOUNTS is REQUIRED, not optional: a PAT machine-user account (type
# "User", no "[bot]" suffix — like the former HOSWorkerTutelare/HOSOversightTutelare
# accounts) can only be excluded by enumerating it here. If BOT_ACCOUNTS is
# empty/unset when a protected surface is touched, require_human_approval.py
# FAILS CLOSED (exit 2, misconfiguration) rather than counting bot approvals as
# human. Fill these in at install time.
BOT_WORKER_USERNAME="hos-worker-hos[bot]"      # GitHub App: hos-worker-hos (app_id: 4090164)
BOT_OVERSEER_USERNAME="hos-overseer-hos[bot]"  # GitHub App: hos-overseer-hos (app_id: 4090678)
# copilot[bot] is GitHub's native AI reviewer. Its APPROVED review is advisory only and MUST NOT
# satisfy the human-approval gate — so it is a BOT identity here, never counted as human.
COPILOT_BOT_LOGIN="copilot[bot]"
# Space-separated set the status check treats as "bot, not human":
BOT_ACCOUNTS="${BOT_WORKER_USERNAME} ${BOT_OVERSEER_USERNAME} ${COPILOT_BOT_LOGIN}"

# ── Overseer auto-approve ceiling (§7 trust ratchet, #167) ────────────────────
# The highest validated risk tier the OVERSEER bot may auto-approve + merge
# end-to-end. Above this, a HUMAN approval is required (server-side).
# Raised to HIGH for v0.4.0 by ScottThurlow authorization (2026-06-19, #598/#600).
# LOW/MEDIUM/HIGH tiers: overseer approves + merges autonomously.
# CRITICAL: overseer adds ScottThurlow as required reviewer; merges after his approval.
# OVERSEER_CEILING options: LOW | MEDIUM | HIGH (never CRITICAL)
OVERSEER_CEILING="HIGH"
# Status check name the merge_authority.py gate detection looks for
TIER_CEILING_CHECK_NAME="require-tier-ceiling"

# ── Human reviewer identity (OM §1, #222) ────────────────────────────────────
# GitHub login of the human who receives reviewer-request on CONDITIONAL_PROCEED
# PRs. Read by oversight-orchestrator.md when posting REQUEST_CHANGES threads.
# Fail-closed if absent: the orchestrator must not skip the reviewer request.
HUMAN_REVIEWER="ScottThurlow"
