# Self + Third-Party Eval — 2026-06-14 (07:00 PDT)

Cross-vendor review of HOS (agy/Gemini + codex/OpenAI). Run by the scheduled 7am eval job.
**Triage discipline applied: every actionable finding verified against the code before acting** (3 of 4 consumer field reports last night were non-reproducing).

## TL;DR
- **Both reviewers: the framework is coherent and architecturally sound.** agy: "highly coherent and well-conceived… the independence principle is exceptionally clean." codex: "coherent in architecture and mostly consistent with its stated oversight model."
- **1 clear bug fixed + merged:** installer copied the source `.venv` into target repos (agy HIGH) → **PR #178 (merged)**.
- **The rest are contract/format drift** (governance) → consolidated for human triage, **not** auto-fixed.
- **Verified-non-reproducing:** codex's "`prompt-fidelity.md` is missing" — the file exists; `review_self.sh` just doesn't bundle it (review-scope artifact).

---

## 1. Self-review — agy (Gemini)
Token use: ~61k. Verdict: coherent framework, 2 HIGH/CRITICAL + 5 MEDIUM.

| Sev | Finding | Disposition |
|---|---|---|
| **HIGH** | Installer copies source `.venv`/`__pycache__` into target → broken Python env | ✅ **Reproduced → fixed, PR #178 merged** |
| CRITICAL | "Base dev/test agents never instructed to write sign-offs to the register" | ⚠️ Partly a review-scope artifact (HOS ships only the 6 oversight agents; base agents are the consumer's). Real residual = no fixture verifying base agents write sign-offs → **needs-human** |
| MEDIUM | Sign-off register schema missing `Reason` field for `Status: N/A` | → needs-human (contract) |
| MEDIUM | `risk-assessment` naming/inclusion in register Roles table | → needs-human (contract) |
| MEDIUM | **DECISIONS.md vs OVERSIGHT-CONTRACT.md contradiction** on missing `risk-assessment.md`: WARN+fallback vs hard FAIL | → needs-human (genuine contradiction) |
| MEDIUM | `spec-red-team` files spec-gap issues but no agent consumes them | overlaps #85/#131 |
| MEDIUM | "never lowers tier" not mechanically enforced | overlaps #94 |

## 2. Self-review — codex (OpenAI)
Token use: ~121k (runs hot/adversarial). ~8 HIGH + several MEDIUM + several LOW "no fix required" confirmations.

**Verified non-reproducing (do not act):**
- "`prompt-fidelity.md` is missing" — **file exists** (3.7 KB); `review_self.sh` only bundles 6 agents (line 91) and omits it. Reviewer inferred "missing" from "not supplied." Same class as last night's phantoms. *(Minor real residual: the review bundle is incomplete → optional improvement to `review_self.sh`.)*

**Contract/format drift (governance → human triage, not auto-fixed):**
| Sev | Finding |
|---|---|
| HIGH | `run_second_review.sh` issue titles/footers don't match contract `[AI: {agent}]` format |
| HIGH | `spec-red-team` issue body uses different fields than the schema it declares |
| HIGH ×2 | Evaluator Phase-1 doesn't enforce role-specific required field `Critical_findings_resolved` for security/privacy |
| MEDIUM | `run_red_team.sh` issue footer missing the contract-required `| YYYY-MM-DD` date |
| MEDIUM | `run_second_review.sh` doesn't write `validated_tier:` in its header, but the evaluator cross-checks that field |
| MEDIUM | prose second-review findings can fall out of the audit trail (not converted to issues) — overlaps #78 |
| MEDIUM | `reliability` role defined but no manifest example — overlaps #135 |

**codex "no fix required" confirmations (positive signal):** RN no longer double-counts nested functions; `run_validators.sh` fail-closes correctly; `token_tracker --all` safe; second-review fallback avoids running codex twice.

---

## 3. Verification tally (verify-first) — a clean vendor-asymmetry signal
Of the findings I verified against the code:
- **agy: 2/2 REAL** — `.venv` HIGH reproduced; `DECISIONS.md:252` staleness is a genuine contradiction with §7a.
- **codex: 0/3 survived** — `prompt-fidelity.md` "missing" (exists; bundle omits it); `Critical_findings_resolved` "not Phase-1-enforced" (contract §3:155 explicitly makes it a **Phase-2** signal, evaluator complies); `validated_tier` "not written" (it's at `run_second_review.sh:129`).

Same code, same task: **codex runs hot and confabulates; agy is precise.** Refines the 2026-06-13 reviewer-hallucination finding — the false-report rate is **vendor-asymmetric**, which should weight how much each verdict is trusted at face value.

## 4. Actions taken
- ✅ **PR #178 merged** — installer `.venv`/`__pycache__` exclusion (the one clear, safe, reproduced, non-governance bug).
- ✅ **PR #179 (review)** — `ops-reviewer.md` 2 path FAILs from the framework static check.
- 📋 **Issue #180 (needs-human)** — the verified `DECISIONS.md` staleness + the (low-confidence) codex contract-format candidates, honestly framed.
- 🚫 Did **not** act on the 3 debunked codex findings or the review-scope base-agent artifact (governance; verify-first).

## 5. Framework validation (run_framework_validation.sh)
The **static check (`check_agents_static.sh`) found 2 path FAILs in `ops-reviewer.md`** (`§1` inside backticks; bare `TELEMETRY-SPEC.md`) and **correctly gated the AI-review phase** — agy/codex framework review did not run because static must pass first. (That gating is the gate working as designed, a positive signal.) Both FAILs fixed → **PR #179**; static check now PASSES (0 findings). The cross-vendor *semantic* coverage was already provided by the self-review above, so a re-run of the AI phase isn't urgent — it can run once #179 merges.

## Token cost
agy ~61k + codex ~121k (self-review) = ~182k. codex ≈ 24% of the $20/mo reserve for one pass — its higher output (and confabulation rate) is the expensive half.
