# oversight runtime state — never commit
.claudetmp/
.ai-local/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
dist/
build/

# secrets
.env
.env.*
!.env.example
*.salt

# OS
.DS_Store
Thumbs.db

# editors
.idea
.vscode
*.swp

# test artifacts
.coverage
htmlcov/
.mutmut-cache

# Python tool caches (regenerated by gates: mypy/pytest/ruff)
.mypy_cache/
.pytest_cache/
.ruff_cache/

# Claude Code machine-local settings — host-specific paths, never commit
.claude/settings.local.json
.claude/hos-sandbox.values
.claude/settings.local.json.bak-*
.claude/hooks
.claude/launch.json
.claude/loop.md
.claude/output-styles
.claude/routines
.claude/scheduled_tasks.json
.claude/skills
.claude/workflows
.mcp.json

# shell/environment dotfiles — machine-local, not part of the product
.bashrc
.bash_profile
.profile
.zprofile
.zshrc
.gitconfig
.ripgreprc
.gitmodules
nohup.out

# direnv — .envrc is a KEYS file on real machines (it sources parent env and
# exports credentials) and is NOT part of the project. Never commit it, nor any
# local variant (.envrc.local, .envrc.prod, ...). Copy .envrc.example instead.
.envrc*
!.envrc.example
