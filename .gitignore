# oversight runtime state — never commit
.claudetmp/
.ai-local/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
dist/
build/

# secrets
.env
.env.*
!.env.example
*.salt

# OS
.DS_Store
Thumbs.db

# editors
.idea/
.vscode/
*.swp

# test artifacts
.coverage
htmlcov/
.mutmut-cache

# Python tool caches (regenerated by gates: mypy/pytest/ruff)
.mypy_cache/
.pytest_cache/
.ruff_cache/

# Validation stamps — causes merge conflicts (#422), redesign in progress
scripts/framework/validation-stamps/

# Audit logs — append-only operational files that cause merge conflicts when
# included in feature PRs. Synced directly to main by _sync_audit_logs() in
# hos-cron after every cycle (git add -f, bypasses PR flow). See #854.
audit/oversight-log.jsonl
audit/overnight-loop-log.md
