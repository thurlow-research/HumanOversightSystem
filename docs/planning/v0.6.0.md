# v0.6.0 — Astro & JS Support: Release Plan

**Theme:** Give an Astro / JS-TS consumer the same deterministic signal density and stack depth a Django consumer gets today.  
**Status:** 🔄 Active (accelerated for the tutelare.ai website build)  
**Milestone:** [v0.6.0 — Astro & JS Support](https://github.com/thurlow-research/HumanOversightSystem/milestone/10)  
**Epic:** #1029  
**Inserted:** 2026-07-26 — prioritized ahead of Quality (now v0.7.0) because an Astro-capable HOS is needed to design and build the tutelare.ai website. Downstream releases cascaded: Quality v0.6.0 → v0.7.0, Agility v0.7.0 → v0.8.0, Feature Enhancements v0.8.0 → v0.9.0; v1.0 unchanged.

---

## Theme rationale

The HOS oversight loop (worker/overseer, contract, review chain, second review, panel, red-team) is already stack-agnostic. What is Django-specific is the **deterministic signal layer**: on an all-`.astro`/`.ts`/`.js` changeset every `PY_FILES`-gated validator skips, so ~half the 12 scoring dimensions go dark, and there is no `packs/astro` giving the base team JS/Astro idioms. v0.6.0 closes that gap with a **two-layer pack** — a generic `node` pack (reusable for future Next/SvelteKit) plus an `astro` pack layered on it via `pack.toml requires = ["node"]`.

---

## Epic-level acceptance criteria

- **AC-1 (signal density):** on an all-JS/TS changeset, ≥10 of 12 scoring dimensions emit real (non-skipped) signal.
- **AC-2 (Python regression):** Python-only changesets score byte-identical to pre-change `main`.
- **AC-3 (schema stability):** new validators emit the shared `schema.py` envelope; weights/thresholds unchanged or logged in `DECISIONS.md`.
- **AC-4 (gates):** eslint + `astro check`/`tsc` run and block in an Astro project; every gate no-ops cleanly on non-JS projects.
- **AC-5 (pack install):** `hos_install.sh --pack astro` resolves + injects `node`; `--pack node` installs standalone.
- **AC-6 (dogfood):** a field report from a real Astro project exists before close.

---

## Build steps (see #1029 for the full per-step spec)

### Foundation
- **S1** — language-aware `JS_FILES` routing in `run_validators.sh` (HIGH; root dependency for all validators)
- **S2** — JS toolchain discovery helper (`ensure_node` analog to `ensure_venv.sh`)
- **S3** — installer `pack.toml requires` resolution (HIGH; **net-new** — no `requires` parsing exists today)

### JS/TS validators
- **S4** complexity · **S5** function-metrics · **S6** static-analysis · **S7** hallucination + `package.json` existence check · **S8** RN JS calibration · **S9** N+1 analog

### Gates
- **S10** lint (eslint) · **S11** type (`astro check`/`tsc`) · **S12** new `astro_check.sh`

### Pack agent depth
- **S13-S15** `packs/node/` (scaffold+core, review lenses, test+UI)
- **S16-S18** `packs/astro/` (scaffold+core, review lenses, test+UI)

### Validation & docs
- **S19** docs (+ `portability_check` if a gap is found) · **S20** dogfood field report (last)

---

## Recommended build order

| Wave | Steps | Notes |
|------|-------|-------|
| 1 | S1, S2, S3, S13 | Foundation; S1 blocks all validators, S3 blocks astro install |
| 2 | S4-S7, S9, S10-S12, S14, S15 | Up to ~8 concurrent PRs |
| 3 | S8, S16 | S8 after S4+S5; S16 after S13+S3 |
| 4 | S17, S18 | astro review-lens + test/UI (after S16 + node counterparts) |
| 5 | S19, S20 | docs, then dogfood last |

**Critical path:** S1 → {S4,S5} → S8, and S3 → S16 → {S17,S18} → S20.

---

## Open decisions (resolve before dependent steps code)

| ID | Decision | Blocks | Owner |
|----|----------|--------|-------|
| D1 | Node baseline: force-install in `hos_bootstrap` vs resolve from consumer toolchain | S2, S10-S12 | **human** |
| D2 | JS tool provisioning model (npx / pinned node_modules / discover-only) | S2 | architect |
| D3 | Installer `requires` scope; widen `config.sh PACK=`? (confirmed net-new) | S3, S16 | architect |
| D4 | JS complexity tool (escomplex vs eslint `complexity` rule) | S4 | architect |
| D5 | JS/TS AST lib + `.astro` frontmatter extraction | S5 | architect |
| D6 | Static-analysis engine: semgrep vs eslint-plugin-security | S6 | architect (+ human on install-weight) |
| D7 | RN nesting calibration for JS: provisional vs gather data | S8 | **human** |
| D8 | N+1 analog: fetch-in-loop heuristic vs explicit N/A | S9 | architect |
| D9 | `astro_check.sh` mechanism (`astro sync`+check vs dry-run build) | S12 | architect |
| **D10** | **Where JS validators live: shared + language-gated (rec) vs pack-discovered — shapes S4-S9** | S4-S9 | architect (**resolve first**) |

---

## Related

- Epic: #1029
- Pack template to mirror: `packs/django/` (pack.toml + 12 agent region-body files)
- Signal-layer entry point: `scripts/oversight/run_validators.sh` (PY_FILES gating)
- Decomposition source: pm-agent scoping, 2026-07-26
